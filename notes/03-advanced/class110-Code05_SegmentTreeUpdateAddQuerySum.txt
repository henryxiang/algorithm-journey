// 线段树同时支持范围重置、范围增加、范围查询
// 维护累加和
// 对数器验证
// 当你写线段树出错了，就需要用对数器的方式来排查
// 所以本题选择对数器验证，来展示一下怎么写测试
		// 生成随机值填入arr数组
		// 建立线段树
		// 生成验证的结构
			// 生成操作类型
			// op = 0 增加操作
			// op = 1 更新操作
			// op = 2 查询操作
			// 下标从1开始，不从0开始，生成两个随机下标
			// 确保jobl <= jobr
				// 增加操作
				// 线段树、验证结构同步增加
				// 更新操作
				// 线段树、验证结构同步更新
				// 查询操作
				// 线段树、验证结构同步查询
				// 比对答案
	// 生成随机值填入arr数组
	// 为了验证
	// 验证结构的增加
	// 暴力增加
	// 为了验证
	// 验证结构的更新
	// 暴力更新
	// 为了验证
	// 验证结构的查询
	// 暴力查询
	// 为了验证
