	// 给定一个正数x
	// 已知用m个二进制位一定能表示x
	// 从高位到低位打印x每一位的状态
	// 给定一个正数x
	// 打印<=x最大的2的幂
	// 到底是2的几次方
		// 以下注释掉的写法不对，没有考虑溢出
//		while ((1 << power) <= x) {
//			power++;
//		}
//		power--;
		// 防止溢出的写法
		// 需要保证用m个二进制位一定能表示x
